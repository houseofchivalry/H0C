H0C v25 - THIN FRAMES ONLY

This package changes ONLY the decorative framing of the H0C home pages.

What changed:
- Removed the large ornate hero frame overlay.
- Removed the ornate button frame.
- Removed the large ornate outer panel frame from the card section.
- Replaced them with narrow 1px gold borders using the existing gold color: #d9a940.
- Removed the large ornate card borders and kept only narrow gold edges.
- Kept the existing hero artwork, values section, text, navigation, links, footer and overall layout.
- Polish home quote remains visible as HTML text instead of being baked into the panel image.
- EN / DE / RU content remains unchanged.
- CSS cache version is v25 on PL/EN/DE/RU home pages.

Upload:
1. Open the root of the H0C GitHub Pages repository.
2. Replace only the files included in this package.
3. Do NOT delete rules.html, events.html or materialy.html.
4. Do NOT upload this ZIP as a file. Upload its contents directly to the repository root.

The package intentionally does not include the old ornate frame assets because they are no longer used by the home pages.
