H0C HOMEPAGE - FINAL V18

This package contains the corrected homepage files for the House of Chivalry site.

IMPORTANT:
- Upload every file from this ZIP to the ROOT of the H0C GitHub repository.
- Replace the existing files with the same names.
- DO NOT delete rules.html, events.html or materialy.html already present in the repository.
- The new files ornate-frame.svg and button-ornate-frame.svg are required and must also be uploaded to the repository root.

What was corrected in V18:
- The hero frame is now a real ornate gold frame matching the approved visualization style: double gold lines, decorated corners and top/bottom center ornaments.
- The lower parchment/card panel uses the same frame design.
- The JOIN button uses a matching decorative gold frame.
- The frame is an overlay, so it does not change the approved section dimensions or content placement.
- All four language pages use the same frame assets and structure.
- Existing approved imagery, typography, text and responsive 1360px composition were preserved.

After uploading:
1. Wait for GitHub Pages deployment to finish.
2. Open the site in Chrome.
3. Use Ctrl+Shift+R for a hard refresh.
4. Check the Polish page first, then EN/DE/RU.

Verification performed before packaging:
- V18 full static verification: PASSED
- V18 visual/frame contract: PASSED
- V15 compatibility checks: PASSED
- Scaling check: PASSED
- All required raster asset dimensions: PASSED
- All local homepage references checked: PASSED (existing repository pages rules.html/events.html/materialy.html intentionally excluded because they are preserved rather than included here)
- SVG frame assets rendered independently for visual inspection: PASSED

Note: the local environment could not complete a headless Chrome page render, so no claim is made that a local browser screenshot was successfully rendered. The frame SVGs and all static structure/assets were independently checked.
