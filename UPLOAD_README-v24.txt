H0C V24 - FINAL UPLOAD

Upload the files from this folder directly into the H0C repository root. Replace same-named files. Do not upload this folder or the ZIP itself.

V24 keeps the approved V23 visual assets and translations, but fixes the shared desktop geometry to the approved reference width: 1392px, centered. The old fluid 90.625vw desktop width is removed. This prevents the hero, values section, panel, typography and frame from enlarging with wide screens.

Verified: four home languages, cache bust v24, hero frame asset, button frame asset, localized values asset, localized panel asset, and ZIP integrity.
